<UserControl x:Class="BioDesk.App.Views.Abas.IrisdiagnosticoUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:BioDesk.App.Converters"
             mc:Ignorable="d"
             d:DesignHeight="800"
    d:DesignWidth="1400"
             Background="{StaticResource FundoPrincipal}">

  <UserControl.Resources>
    <converters:PathToImageSourceConverter x:Key="PathToImageConverter"/>
    <!--  BooleanToVisibilityConverter já definido no App.xaml linha 13  -->

    <!-- Converter: Percentual (0-100) para Opacity (0-1) -->
    <converters:PercentToOpacityConverter x:Key="PercentToOpacityConverter"/>

    <!-- Converter: Raio para Diâmetro (raio * 2) -->
    <converters:DiameterConverter x:Key="DiameterConverter"/>

    <!-- Converter: List<SimplePoint> para PointCollection -->
    <converters:SimplePointCollectionConverter x:Key="SimplePointCollectionConverter"/>

    <!-- Converter: Centro para Offset (centro - raio) -->
    <converters:CenterOffsetConverter x:Key="CenterOffsetConverter"/>

    <!-- Converter: Bool para Texto Personalizado -->
    <converters:BoolToTextConverter x:Key="BoolToTextConverter"/>

    <!-- Converter: Índice (1-8) para Nome do Ponto Cardeal -->
    <converters:IndexToCardinalConverter x:Key="IndexToCardinalConverter"/>
  </UserControl.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="1*"/>
      <!-- Direito: ~20% -->
      <ColumnDefinition Width="2.5*"/>
      <!-- Mapa Central: ~50% (mais quadrado) -->
      <ColumnDefinition Width="1.5*"/>
      <!-- Esquerdo: ~30% -->
    </Grid.ColumnDefinitions>

    <!-- COLUNA 0: DIREITO - Galeria de Imagens -->
    <Border Grid.Column="0"
            Background="#F7F9F6"
            CornerRadius="8"
            Padding="12"
            Margin="0,0,8,0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0"
                   Text="📸 Imagens de Íris"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#3F4A3D"
                   Margin="0,0,0,12"/>

        <!-- ListBox de Imagens -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding IrisImagens}"
                 SelectedItem="{Binding IrisImagemSelecionada}"
                 Background="Transparent"
                 BorderThickness="0">
          <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="ListBoxItem">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                      <ContentPresenter/>
                    </Border>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
              <Setter Property="Background"
                  Value="Transparent"/>
              <Setter Property="BorderBrush"
                  Value="Transparent"/>
              <Setter Property="BorderThickness"
                  Value="2"/>
              <Setter Property="Padding"
                  Value="0"/>
              <Setter Property="Margin"
                  Value="0,0,0,4"/>
              <Style.Triggers>
                <Trigger Property="IsSelected"
                    Value="True">
                  <Setter Property="BorderBrush"
                      Value="#6B8E63"/>
                  <Setter Property="BorderThickness"
                      Value="3"/>
                </Trigger>
                <Trigger Property="IsMouseOver"
                    Value="True">
                  <Setter Property="BorderBrush"
                      Value="#9CAF97"/>
                  <Setter Property="BorderThickness"
                      Value="2"/>
                </Trigger>
              </Style.Triggers>
            </Style>
          </ListBox.ItemContainerStyle>
          <ListBox.ItemTemplate>
            <DataTemplate>
              <Border Background="White"
                      CornerRadius="6"
                      Padding="8"
                      Margin="0">
                <Grid>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                  </Grid.RowDefinitions>

                  <TextBlock Grid.Row="0"
                             Text="{Binding Olho}"
                             FontSize="14"
                             FontWeight="SemiBold"
                             Foreground="#3F4A3D"/>

                  <TextBlock Grid.Row="1"
                             Text="{Binding DataCaptura, StringFormat='dd/MM/yyyy HH:mm'}"
                             FontSize="12"
                             Foreground="#5A6558"
                             Margin="0,4,0,0"/>

                  <TextBlock Grid.Row="2"
                             Text="{Binding Observacoes}"
                             FontSize="11"
                             Foreground="#5A6558"
                             TextWrapping="Wrap"
                             Margin="0,4,0,0"/>
                </Grid>
              </Border>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>

        <!-- Botões de Ação: Grid 3 colunas iguais -->
        <Grid Grid.Row="2"
            Margin="0,12,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <!-- Botão Adicionar (verde claro) -->
          <Button Grid.Column="0"
                  Content="📁"
                  Command="{Binding AdicionarImagemCommand}"
                  Background="#9CAF97"
                  Foreground="White"
                  Padding="8"
                  BorderThickness="0"
                  FontSize="18"
                  FontWeight="Bold"
                  Cursor="Hand"
                  ToolTip="Adicionar imagem de ficheiro"
                  Margin="0,0,2,0">
            <Button.Style>
              <Style TargetType="Button">
                <Setter Property="Template">
                  <Setter.Value>
                    <ControlTemplate TargetType="Button">
                      <Border Background="{TemplateBinding Background}"
                              CornerRadius="6"
                              Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                      </Border>
                    </ControlTemplate>
                  </Setter.Value>
                </Setter>
                <Style.Triggers>
                  <Trigger Property="IsMouseOver"
                      Value="True">
                    <Setter Property="Background"
                        Value="#879B83"/>
                  </Trigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
          </Button>

          <!-- Botão Capturar (verde escuro) -->
          <Button Grid.Column="1"
                  Content="📷"
                  Click="CapturarDaCameraButton_Click"
                  Background="#6B8E63"
                  Foreground="White"
                  Padding="8"
                  BorderThickness="0"
                  FontSize="18"
                  FontWeight="Bold"
                  Cursor="Hand"
                  ToolTip="Capturar foto de íris com câmera"
                  Margin="2,0">
            <Button.Style>
              <Style TargetType="Button">
                <Setter Property="Template">
                  <Setter.Value>
                    <ControlTemplate TargetType="Button">
                      <Border Background="{TemplateBinding Background}"
                              CornerRadius="6"
                              Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                      </Border>
                    </ControlTemplate>
                  </Setter.Value>
                </Setter>
                <Style.Triggers>
                  <Trigger Property="IsMouseOver"
                      Value="True">
                    <Setter Property="Background"
                        Value="#5A7853"/>
                  </Trigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
          </Button>

          <!-- Botão Remover (vermelho terroso) -->
          <Button Grid.Column="2"
                  Content="❌"
                  Command="{Binding RemoverImagemCommand}"
                  Background="#C85959"
                  Foreground="White"
                  Padding="8"
                  BorderThickness="0"
                  FontSize="18"
                  FontWeight="Bold"
                  Cursor="Hand"
                  ToolTipService.ToolTip="Remover imagem selecionada"
                  Margin="2,0,0,0">
            <Button.Style>
              <Style TargetType="Button">
                <Setter Property="Template">
                  <Setter.Value>
                    <ControlTemplate TargetType="Button">
                      <Border Background="{TemplateBinding Background}"
                              CornerRadius="6"
                              Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                      </Border>
                    </ControlTemplate>
                  </Setter.Value>
                </Setter>
                <Style.Triggers>
                  <Trigger Property="IsMouseOver"
                      Value="True">
                    <Setter Property="Background"
                        Value="#A84545"/>
                  </Trigger>
                  <Trigger Property="IsEnabled"
                      Value="False">
                    <Setter Property="Opacity"
                        Value="0.4"/>
                    <Setter Property="Cursor"
                        Value="Arrow"/>
                  </Trigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
          </Button>
        </Grid>
      </Grid>
    </Border>

    <!-- COLUNA 1: PREVIEW CENTRAL + MAPA IRIDOLÓGICO (50% largura) -->
    <Border Grid.Column="1"
            Background="#F7F9F6"
            CornerRadius="8"
            Padding="0"
            Margin="8,0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <!-- Painel de Instruções -->
          <RowDefinition Height="*"/>
          <!-- Canvas/Viewbox -->
        </Grid.RowDefinitions>

        <!-- 🎯 NOVO: Painel de Instruções de Calibração -->
        <Border Grid.Row="0"
                Background="#FFE4CC"
                BorderBrush="#FF9800"
                BorderThickness="0,0,0,2"
                Padding="16,12"
                CornerRadius="8,8,0,0">
          <Border.Style>
            <Style TargetType="Border">
              <Setter Property="Visibility" Value="Collapsed"/>
              <Style.Triggers>
                <DataTrigger Binding="{Binding IsAlignmentActive}" Value="True">
                  <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Border.Style>
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="🎯" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding AlignmentInstructionText}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="#BF5E00"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"/>
          </StackPanel>
        </Border>

        <!-- Canvas Central com Imagem + Mapa Sobreposto -->
        <Viewbox Grid.Row="1" Stretch="Uniform">
          <Canvas Width="1400"
              Height="1400"
              Background="White">

            <!-- LAYER 0: Placeholder quando não há imagem selecionada ✅ AUDITADO -->
            <TextBlock Panel.ZIndex="0"
                       Canvas.Left="200"
                Canvas.Top="650"
                       Width="1000"
                       Text="📷 Selecione uma imagem de íris na galeria à esquerda"
                       FontSize="48"
                       Foreground="#9CAF97"
                       TextAlignment="Center"
                       TextWrapping="Wrap">
              <TextBlock.Style>
                <Style TargetType="TextBlock">
                  <Setter Property="Visibility"
                      Value="Collapsed"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IrisImagemSelecionada}"
                        Value="{x:Null}">
                      <Setter Property="Visibility"
                          Value="Visible"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </TextBlock.Style>
            </TextBlock>

            <!-- LAYER 1: Imagem da Íris Real (sempre visível) -->
            <Image x:Name="IrisCentralImage"
                   Width="1400"
                Height="1400"
                   Source="{Binding IrisImagemSelecionada.CaminhoImagem, Converter={StaticResource PathToImageConverter}}"
                   Stretch="Uniform"
                   Panel.ZIndex="1">
              <Image.Style>
                <Style TargetType="Image">
                  <Setter Property="Visibility"
                      Value="Visible"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IrisImagemSelecionada}"
                        Value="{x:Null}">
                      <Setter Property="Visibility"
                          Value="Collapsed"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Image.Style>
            </Image>

            <!-- LAYER 2: Mapa Iridológico (polígonos com transformação do IrisOverlayService) -->
            <Canvas x:Name="MapaOverlayCanvas"
                    Width="1400"
                Height="1400"
                    Panel.ZIndex="2"
                    Background="Transparent"
                    MouseLeftButtonDown="MapaOverlayCanvas_Click">
              <!-- Transformação aplicada pelo IrisOverlayService (3-click + OpenCV) -->
              <Canvas.RenderTransform>
                <Binding Path="OverlayTransform">
                  <Binding.FallbackValue>
                    <TransformGroup>
                      <TranslateTransform X="0" Y="0"/>
                      <ScaleTransform ScaleX="1" ScaleY="1" CenterX="700" CenterY="700"/>
                    </TransformGroup>
                  </Binding.FallbackValue>
                </Binding>
              </Canvas.RenderTransform>
              <Canvas.Style>
                <Style TargetType="Canvas">
                  <Setter Property="Visibility" Value="Collapsed"/>
                  <Setter Property="Cursor" Value="Cross"/>
                  <!-- Captura cliques quando alinhamento está ativo -->
                  <Setter Property="IsHitTestVisible" Value="False"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding MostrarMapaIridologico}" Value="True">
                      <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                    <!-- Ativa captura de cliques durante alinhamento -->
                    <DataTrigger Binding="{Binding IsAlignmentActive}" Value="True">
                      <Setter Property="IsHitTestVisible" Value="True"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Canvas.Style>

              <!-- Polígonos das zonas -->
              <ItemsControl ItemsSource="{Binding PoligonosZonas}"
                            IsHitTestVisible="False">
                <ItemsControl.ItemsPanel>
                  <ItemsPanelTemplate>
                    <Canvas/>
                  </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Polygon Points="{Binding Pontos}"
                             Stroke="#2C3E50"
                             StrokeThickness="1.5"
                             Fill="{Binding CorPreenchimento}"
                             Cursor="Hand"
                             StrokeLineJoin="Round"
                             StrokeMiterLimit="3">
                      <!-- ✨ MELHORIA: Efeito visual para polígonos -->
                      <Polygon.Effect>
                        <DropShadowEffect Color="#000000"
                                          BlurRadius="2"
                                          Opacity="0.15"
                                          ShadowDepth="1"/>
                      </Polygon.Effect>
                      <!-- Tooltip com nome e descrição -->
                      <Polygon.ToolTip>
                        <ToolTip MaxWidth="300"
                            Background="#F7F9F6"
                            Foreground="#3F4A3D"
                            BorderBrush="#E3E9DE"
                            BorderThickness="1">
                          <StackPanel>
                            <TextBlock Text="{Binding Nome}"
                                FontWeight="SemiBold"
                                FontSize="12"
                                Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding Descricao}"
                                FontSize="11"
                                TextWrapping="Wrap"/>
                          </StackPanel>
                        </ToolTip>
                      </Polygon.ToolTip>
                      <Polygon.Style>
                        <Style TargetType="Polygon">
                          <Setter Property="Opacity"
                              Value="{Binding DataContext.OpacidadeMapa, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource PercentToOpacityConverter}}"/>
                          <Style.Triggers>
                            <Trigger Property="IsMouseOver"
                                Value="True">
                              <Setter Property="Opacity"
                                  Value="0.9"/>
                              <Setter Property="StrokeThickness"
                                  Value="2.5"/>
                              <Setter Property="Stroke"
                                  Value="#E74C3C"/>
                              <!-- ✨ MELHORIA: Animação suave -->
                              <Setter Property="RenderTransform">
                                <Setter.Value>
                                  <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                                </Setter.Value>
                              </Setter>
                            </Trigger>
                          </Style.Triggers>
                        </Style>
                      </Polygon.Style>
                    </Polygon>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </Canvas>

            <!-- LAYER 4: Canvas de Desenho (visível só em modo desenho) -->
            <Canvas x:Name="DesenhoCanvas"
                    Width="1400"
                Height="1400"
                    Panel.ZIndex="4"
                    Background="Transparent"
                    MouseLeftButtonDown="DesenhoCanvas_MouseLeftButtonDown"
                    MouseMove="DesenhoCanvas_MouseMove"
                    MouseLeftButtonUp="DesenhoCanvas_MouseLeftButtonUp"
                    MouseLeave="DesenhoCanvas_MouseLeave">
              <Canvas.Style>
                <Style TargetType="Canvas">
                  <!-- Hit-test apenas quando modo desenho ativo -->
                  <Setter Property="IsHitTestVisible"
                      Value="False"/>
                  <Setter Property="Visibility"
                      Value="Collapsed"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding ModoDesenhoAtivo}"
                        Value="True">
                      <Setter Property="IsHitTestVisible"
                          Value="True"/>
                      <Setter Property="Visibility"
                          Value="Visible"/>
                      <Setter Property="Cursor"
                          Value="Cross"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Canvas.Style>

              <!-- Renderização dos strokes -->
              <ItemsControl ItemsSource="{Binding Strokes}">
                <ItemsControl.ItemsPanel>
                  <ItemsPanelTemplate>
                    <Canvas/>
                  </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Polyline Points="{Binding Points, Converter={StaticResource SimplePointCollectionConverter}}"
                              Stroke="{Binding Color}"
                              StrokeThickness="{Binding Thickness}"
                              StrokeLineJoin="Round"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              IsHitTestVisible="False"/>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </Canvas>

            <!-- LAYER 6: Canvas para capturar clicks gerais / marcações (necessário para ProcessarCliqueNaMarcacao) -->
            <Canvas x:Name="MarkingsCanvas"
                    Width="1400"
                    Height="1400"
                    Panel.ZIndex="300"
                    Background="Transparent"
                    MouseLeftButtonDown="MarkingsCanvas_MouseLeftButtonDown"
                    IsHitTestVisible="True">
              <Canvas.Style>
                <Style TargetType="Canvas">
                  <Setter Property="Visibility" Value="Visible"/>
                  <Style.Triggers>
                    <!-- Ocultar quando não há imagem selecionada -->
                    <DataTrigger Binding="{Binding IrisImagemSelecionada}" Value="{x:Null}">
                      <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Canvas.Style>
            </Canvas>

            <!-- LAYER 4.5: Pontos Visuais de Marcação (feedback durante marcação 8-pontos) -->
            <ItemsControl ItemsSource="{Binding PontosVisuais}"
                          Panel.ZIndex="450"
                          IsHitTestVisible="False">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <Canvas Width="1400" Height="1400"/>
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                  <Setter Property="Canvas.Left" Value="{Binding X}"/>
                  <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                </Style>
              </ItemsControl.ItemContainerStyle>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Grid>
                    <!-- Círculo com número -->
                    <Ellipse Width="32" Height="32"
                             Fill="{Binding Cor}"
                             Stroke="White"
                             StrokeThickness="3"
                             Opacity="0.8"/>
                    <!-- Número do ponto -->
                    <TextBlock Text="{Binding Numero}"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="14"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                  </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- ✨ NOVO: LAYER 4.7: Feedback Visual Imediato (pontos vermelhos nos cliques) -->
            <ItemsControl ItemsSource="{Binding ClicksTemporarios}"
                          Panel.ZIndex="500"
                          IsHitTestVisible="False">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <Canvas Width="1400" Height="1400"/>
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                  <Setter Property="Canvas.Left" Value="{Binding X}"/>
                  <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                </Style>
              </ItemsControl.ItemContainerStyle>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Grid>
                    <!-- Círculo de feedback vermelho brilhante -->
                    <Ellipse Width="24" Height="24"
                             Fill="{Binding Cor}"
                             Stroke="#FFFFFF"
                             StrokeThickness="2"
                             Opacity="{Binding Opacity}">
                      <Ellipse.Effect>
                        <DropShadowEffect Color="#FF6B6B" BlurRadius="8" Opacity="0.6"/>
                      </Ellipse.Effect>
                    </Ellipse>
                  </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- 🎯 NOVO: LAYER 6: Canvas de Calibração 5-Pontos (ZIndex mais alto) -->
            <Canvas x:Name="CalibrationCanvas"
                    Width="1400"
                    Height="1400"
                    Panel.ZIndex="600"
                    Background="Transparent"
                    MouseLeftButtonDown="CalibrationCanvas_MouseLeftButtonDown"
                    IsHitTestVisible="True">
              <Canvas.Style>
                <Style TargetType="Canvas">
                  <!-- Visível apenas quando alinhamento está ativo -->
                  <Setter Property="Visibility" Value="Collapsed"/>
                  <Setter Property="Cursor" Value="Cross"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsAlignmentActive}" Value="True">
                      <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Canvas.Style>
            </Canvas>

            <!-- 🎯 LAYER 6.5: Marcadores Visuais de Calibração (5 pontos) -->
            <ItemsControl ItemsSource="{Binding CalibrationMarkers}"
                          Panel.ZIndex="650"
                          IsHitTestVisible="False">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <Canvas Width="1400" Height="1400"/>
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                  <Setter Property="Canvas.Left">
                    <Setter.Value>
                      <Binding Path="X">
                        <Binding.Converter>
                          <converters:CenterOffsetConverter/>
                        </Binding.Converter>
                        <Binding.ConverterParameter>-16</Binding.ConverterParameter>
                      </Binding>
                    </Setter.Value>
                  </Setter>
                  <Setter Property="Canvas.Top">
                    <Setter.Value>
                      <Binding Path="Y">
                        <Binding.Converter>
                          <converters:CenterOffsetConverter/>
                        </Binding.Converter>
                        <Binding.ConverterParameter>-16</Binding.ConverterParameter>
                      </Binding>
                    </Setter.Value>
                  </Setter>
                </Style>
              </ItemsControl.ItemContainerStyle>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Grid>
                    <!-- Círculo numerado colorido -->
                    <Ellipse Width="32" Height="32"
                             Fill="{Binding Color}"
                             Stroke="White"
                             StrokeThickness="3"
                             Opacity="0.85">
                      <Ellipse.Effect>
                        <DropShadowEffect Color="{Binding Color}" BlurRadius="12" Opacity="0.5"/>
                      </Ellipse.Effect>
                    </Ellipse>
                    <TextBlock Text="{Binding Number}"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="16"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                  </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- LAYER 5: Marcas de Íris (Círculos Coloridos com ContextMenu) -->
            <ItemsControl ItemsSource="{Binding MarcasImagem}"
                          Panel.ZIndex="4">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <Canvas Width="1600"
                      Height="1600"/>
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Ellipse Width="24"
                      Height="24"
                           Fill="{Binding Cor}"
                           Stroke="White"
                           StrokeThickness="2"
                           Cursor="Hand"
                           ToolTip="{Binding Observacoes}">
                    <!-- Posicionamento no canvas -->
                    <Canvas.Left>
                      <Binding Path="X">
                        <Binding.Converter>
                          <converters:CenterOffsetConverter/>
                        </Binding.Converter>
                        <Binding.ConverterParameter>-12</Binding.ConverterParameter>
                      </Binding>
                    </Canvas.Left>
                    <Canvas.Top>
                      <Binding Path="Y">
                        <Binding.Converter>
                          <converters:CenterOffsetConverter/>
                        </Binding.Converter>
                        <Binding.ConverterParameter>-12</Binding.ConverterParameter>
                      </Binding>
                    </Canvas.Top>

                    <!-- Menu Contextual -->
                    <Ellipse.ContextMenu>
                      <ContextMenu Background="#F7F9F6"
                          BorderBrush="#E3E9DE"
                          Padding="4">
                        <!-- Editar Observações -->
                        <MenuItem Header="📝 Editar Observações"
                                  Click="EditarObservacoes_Click">
                          <MenuItem.Style>
                            <Style TargetType="MenuItem">
                              <Setter Property="Background"
                                  Value="Transparent"/>
                              <Setter Property="Foreground"
                                  Value="#3F4A3D"/>
                              <Setter Property="FontSize"
                                  Value="13"/>
                              <Style.Triggers>
                                <Trigger Property="IsMouseOver"
                                    Value="True">
                                  <Setter Property="Background"
                                      Value="#E3E9DE"/>
                                </Trigger>
                              </Style.Triggers>
                            </Style>
                          </MenuItem.Style>
                        </MenuItem>

                        <Separator Background="#E3E9DE"
                            Margin="2,4"/>

                        <!-- Submenu Mudar Cor -->
                        <MenuItem Header="🎨 Mudar Cor">
                          <MenuItem.Style>
                            <Style TargetType="MenuItem">
                              <Setter Property="Background"
                                  Value="Transparent"/>
                              <Setter Property="Foreground"
                                  Value="#3F4A3D"/>
                              <Setter Property="FontSize"
                                  Value="13"/>
                            </Style>
                          </MenuItem.Style>
                          <MenuItem Header="🔴 Vermelho Terroso"
                                    Tag="#C85959"
                                    Click="MudarCor_Click"/>
                          <MenuItem Header="🟢 Verde Musgo"
                                    Tag="#6B8E63"
                                    Click="MudarCor_Click"/>
                          <MenuItem Header="🔵 Azul Petróleo"
                                    Tag="#5B7C99"
                                    Click="MudarCor_Click"/>
                          <MenuItem Header="🟡 Amarelo Mostarda"
                                    Tag="#D4A849"
                                    Click="MudarCor_Click"/>
                        </MenuItem>

                        <Separator Background="#E3E9DE"
                            Margin="2,4"/>

                        <!-- Remover Marca -->
                        <MenuItem Header="🗑️ Remover"
                                  Command="{Binding DataContext.RemoverMarcaCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  CommandParameter="{Binding}">
                          <MenuItem.Style>
                            <Style TargetType="MenuItem">
                              <Setter Property="Background"
                                  Value="Transparent"/>
                              <Setter Property="Foreground"
                                  Value="#C85959"/>
                              <Setter Property="FontSize"
                                  Value="13"/>
                              <Setter Property="FontWeight"
                                  Value="SemiBold"/>
                              <Style.Triggers>
                                <Trigger Property="IsMouseOver"
                                    Value="True">
                                  <Setter Property="Background"
                                      Value="#FFEAEA"/>
                                </Trigger>
                              </Style.Triggers>
                            </Style>
                          </MenuItem.Style>
                        </MenuItem>
                      </ContextMenu>
                    </Ellipse.ContextMenu>

                    <!-- Efeito Hover -->
                    <Ellipse.Style>
                      <Style TargetType="Ellipse">
                        <Style.Triggers>
                          <Trigger Property="IsMouseOver"
                              Value="True">
                            <Setter Property="StrokeThickness"
                                Value="3"/>
                            <Setter Property="Effect">
                              <Setter.Value>
                                <DropShadowEffect Color="Black"
                                    Opacity="0.4"
                                    BlurRadius="8"
                                    ShadowDepth="2"/>
                              </Setter.Value>
                            </Setter>
                          </Trigger>
                        </Style.Triggers>
                      </Style>
                    </Ellipse.Style>
                  </Ellipse>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>

          </Canvas>
        </Viewbox>
      </Grid>
    </Border>

    <!-- COLUNA 2: ESQUERDO - Preview (era Grid.Column="1", agora é "2") -->
    <Border Grid.Column="2"
            Background="#F7F9F6"
            CornerRadius="8"
            Padding="16">
      <!-- ✅ NOVO: ScrollViewer para permitir scroll vertical -->
      <ScrollViewer VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Disabled">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Título -->
            <RowDefinition Height="Auto"/>   <!-- Sistema 8 Pontos -->
            <RowDefinition Height="Auto"/>   <!-- Ferramenta Desenho -->
            <RowDefinition Height="Auto"/>   <!-- Conteúdo restante (mudado de * para Auto) -->
          </Grid.RowDefinitions>

          <!-- Título -->
        <TextBlock Grid.Row="0"
                   Text="🔍 Preview da Íris"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#3F4A3D"
                   Margin="0,0,0,12"/>

        <!-- Ferramenta de Desenho (Caneta) -->
        <Border Grid.Row="2"
            Background="White"
            CornerRadius="8"
            BorderBrush="#E3E9DE"
            BorderThickness="1"
            Padding="12"
            Margin="0,0,0,12">
          <StackPanel>
            <!-- Toggle Modo Desenho -->
            <ToggleButton x:Name="ModoDesenhoToggle"
                          Content="✏️ Modo Desenho"
                          IsChecked="{Binding ModoDesenhoAtivo, Mode=TwoWay}"
                          HorizontalAlignment="Stretch"
                          Height="32"
                          FontSize="12"
                FontWeight="SemiBold"
                          Cursor="Hand"
                          Margin="0,0,0,12">
              <ToggleButton.Style>
                <Style TargetType="ToggleButton">
                  <Setter Property="Background"
                      Value="#E3E9DE"/>
                  <Setter Property="Foreground"
                      Value="#3F4A3D"/>
                  <Setter Property="BorderThickness"
                      Value="0"/>
                  <Setter Property="Template">
                    <Setter.Value>
                      <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                          <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                        </Border>
                      </ControlTemplate>
                    </Setter.Value>
                  </Setter>
                  <Style.Triggers>
                    <Trigger Property="IsChecked"
                        Value="True">
                      <Setter Property="Background"
                          Value="#6B8E63"/>
                      <Setter Property="Foreground"
                          Value="White"/>
                    </Trigger>
                    <Trigger Property="IsMouseOver"
                        Value="True">
                      <Setter Property="Background"
                          Value="#879B83"/>
                    </Trigger>
                  </Style.Triggers>
                </Style>
              </ToggleButton.Style>
            </ToggleButton>

            <!-- Controlos de Desenho (visíveis apenas quando ativo) -->
            <StackPanel Visibility="{Binding ModoDesenhoAtivo, Converter={StaticResource BooleanToVisibilityConverter}}">
              <!-- Seletor de Cor -->
              <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                    Text="🎨 Cor:"
                    FontSize="11"
                    Foreground="#5A6558"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0"/>
                <StackPanel Grid.Column="1"
                    Orientation="Horizontal">
                  <!-- Vermelho Terroso -->
                  <RadioButton x:Name="CorVermelha"
                      GroupName="CorDesenho"
                      IsChecked="True"
                      Width="24"
                      Height="24"
                      Margin="0,0,4,0"
                      Cursor="Hand"
                      Tag="#C85959"
                      Checked="CorDesenho_Checked">
                    <RadioButton.Template>
                      <ControlTemplate TargetType="RadioButton">
                        <Border Background="#C85959"
                            BorderBrush="#E3E9DE"
                            BorderThickness="2"
                            CornerRadius="12"
                            Width="24"
                            Height="24">
                          <Border.Style>
                            <Style TargetType="Border">
                              <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}}"
                                    Value="True">
                                  <Setter Property="BorderBrush"
                                      Value="#3F4A3D"/>
                                  <Setter Property="BorderThickness"
                                      Value="3"/>
                                </DataTrigger>
                              </Style.Triggers>
                            </Style>
                          </Border.Style>
                        </Border>
                      </ControlTemplate>
                    </RadioButton.Template>
                  </RadioButton>
                  <!-- Verde Musgo -->
                  <RadioButton x:Name="CorVerde"
                      GroupName="CorDesenho"
                      Width="24"
                      Height="24"
                      Margin="0,0,4,0"
                      Cursor="Hand"
                      Tag="#6B8E63"
                      Checked="CorDesenho_Checked">
                    <RadioButton.Template>
                      <ControlTemplate TargetType="RadioButton">
                        <Border Background="#6B8E63"
                            BorderBrush="#E3E9DE"
                            BorderThickness="2"
                            CornerRadius="12"
                            Width="24"
                            Height="24">
                          <Border.Style>
                            <Style TargetType="Border">
                              <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}}"
                                    Value="True">
                                  <Setter Property="BorderBrush"
                                      Value="#3F4A3D"/>
                                  <Setter Property="BorderThickness"
                                      Value="3"/>
                                </DataTrigger>
                              </Style.Triggers>
                            </Style>
                          </Border.Style>
                        </Border>
                      </ControlTemplate>
                    </RadioButton.Template>
                  </RadioButton>
                  <!-- Azul Petróleo -->
                  <RadioButton x:Name="CorAzul"
                      GroupName="CorDesenho"
                      Width="24"
                      Height="24"
                      Margin="0,0,4,0"
                      Cursor="Hand"
                      Tag="#5B7C99"
                      Checked="CorDesenho_Checked">
                    <RadioButton.Template>
                      <ControlTemplate TargetType="RadioButton">
                        <Border Background="#5B7C99"
                            BorderBrush="#E3E9DE"
                            BorderThickness="2"
                            CornerRadius="12"
                            Width="24"
                            Height="24">
                          <Border.Style>
                            <Style TargetType="Border">
                              <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}}"
                                    Value="True">
                                  <Setter Property="BorderBrush"
                                      Value="#3F4A3D"/>
                                  <Setter Property="BorderThickness"
                                      Value="3"/>
                                </DataTrigger>
                              </Style.Triggers>
                            </Style>
                          </Border.Style>
                        </Border>
                      </ControlTemplate>
                    </RadioButton.Template>
                  </RadioButton>
                  <!-- Amarelo Mostarda -->
                  <RadioButton x:Name="CorAmarela"
                      GroupName="CorDesenho"
                      Width="24"
                      Height="24"
                      Cursor="Hand"
                      Tag="#D4A849"
                      Checked="CorDesenho_Checked">
                    <RadioButton.Template>
                      <ControlTemplate TargetType="RadioButton">
                        <Border Background="#D4A849"
                            BorderBrush="#E3E9DE"
                            BorderThickness="2"
                            CornerRadius="12"
                            Width="24"
                            Height="24">
                          <Border.Style>
                            <Style TargetType="Border">
                              <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}}"
                                    Value="True">
                                  <Setter Property="BorderBrush"
                                      Value="#3F4A3D"/>
                                  <Setter Property="BorderThickness"
                                      Value="3"/>
                                </DataTrigger>
                              </Style.Triggers>
                            </Style>
                          </Border.Style>
                        </Border>
                      </ControlTemplate>
                    </RadioButton.Template>
                  </RadioButton>
                </StackPanel>
              </Grid>

              <!-- Slider de Espessura -->
              <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto"/>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                    Text="📏 Espessura:"
                    FontSize="11"
                    Foreground="#5A6558"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0"/>
                <Slider x:Name="EspessuraSlider"
                    Grid.Column="1"
                    Minimum="1"
                    Maximum="5"
                    Value="2"
                    TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    VerticalAlignment="Center"
                    ValueChanged="EspessuraDesenho_ValueChanged"/>
                <TextBlock Grid.Column="2"
                    Text="{Binding Value, ElementName=EspessuraSlider, StringFormat='{}{0:F0}px'}"
                    FontSize="11"
                    Foreground="#3F4A3D"
                    VerticalAlignment="Center"
                    Width="30"
                    TextAlignment="Right"
                    Margin="8,0,0,0"/>
              </Grid>

              <!-- Botões Limpar e Desfazer -->
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                    Content="🗑️ Limpar"
                    Command="{Binding LimparDesenhosCommand}"
                        Background="#E3E9DE"
                    Foreground="#3F4A3D"
                    BorderThickness="0"
                    Height="28"
                    FontSize="11"
                    Cursor="Hand"
                    Margin="0,0,4,0">
                  <Button.Style>
                    <Style TargetType="Button">
                      <Setter Property="Template">
                        <Setter.Value>
                          <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                              <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                            </Border>
                          </ControlTemplate>
                        </Setter.Value>
                      </Setter>
                      <Style.Triggers>
                        <Trigger Property="IsMouseOver"
                            Value="True">
                          <Setter Property="Background"
                              Value="#D3D9CE"/>
                        </Trigger>
                      </Style.Triggers>
                    </Style>
                  </Button.Style>
                </Button>
                <Button Grid.Column="1"
                    Content="↶ Desfazer"
                    Command="{Binding DesfazerDesenhoCommand}"
                        Background="#E3E9DE"
                    Foreground="#3F4A3D"
                    BorderThickness="0"
                    Height="28"
                    FontSize="11"
                    Cursor="Hand"
                    Margin="4,0,0,0">
                  <Button.Style>
                    <Style TargetType="Button">
                      <Setter Property="Template">
                        <Setter.Value>
                          <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                              <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                            </Border>
                          </ControlTemplate>
                        </Setter.Value>
                      </Setter>
                      <Style.Triggers>
                        <Trigger Property="IsMouseOver"
                            Value="True">
                          <Setter Property="Background"
                              Value="#D3D9CE"/>
                        </Trigger>
                      </Style.Triggers>
                    </Style>
                  </Button.Style>
                </Button>
              </Grid>
            </StackPanel>
          </StackPanel>
        </Border>

        <!-- 🎯 NOVO: Controles de Alinhamento Overlay (Sistema Infalível) -->
        <Border Grid.Row="2"
            Background="White"
            CornerRadius="8"
            BorderBrush="#E3E9DE"
            BorderThickness="1"
            Padding="16"
            Margin="0,12,0,0">
          <StackPanel>
            <!-- Toggle Mapa Iridológico -->
            <ToggleButton Content="🔍 Mostrar Mapa"
                          IsChecked="{Binding MostrarMapaIridologico, Mode=TwoWay}"
                          HorizontalAlignment="Stretch"
                          Height="36"
                          FontSize="13"
                FontWeight="SemiBold"
                          Cursor="Hand"
                          Margin="0,0,0,16">
              <ToggleButton.Style>
                <Style TargetType="ToggleButton">
                  <Setter Property="Template">
                    <Setter.Value>
                      <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="8"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="2">
                          <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                        </Border>
                      </ControlTemplate>
                    </Setter.Value>
                  </Setter>
                  <Setter Property="Background"
                      Value="#E3E9DE"/>
                  <Setter Property="Foreground"
                      Value="#3F4A3D"/>
                  <Setter Property="BorderBrush"
                      Value="Transparent"/>
                  <Style.Triggers>
                    <Trigger Property="IsChecked"
                        Value="True">
                      <Setter Property="Background"
                          Value="#6B8E63"/>
                      <Setter Property="Foreground"
                          Value="White"/>
                      <Setter Property="BorderBrush"
                          Value="#3F4A3D"/>
                    </Trigger>
                    <Trigger Property="IsMouseOver"
                        Value="True">
                      <Setter Property="Background"
                          Value="#9CAF97"/>
                    </Trigger>
                  </Style.Triggers>
                </Style>
              </ToggleButton.Style>
            </ToggleButton>

            <!-- 🎯 Botão Iniciar Calibração (5-Pontos) -->
            <Button Content="🎯 Iniciar Calibração"
                    Command="{Binding StartAlignmentCommand}"
                    Height="40"
                    Margin="0,0,0,8">
              <Button.ToolTip>
                <TextBlock Text="Inicia o processo de calibração manual de 5 pontos (centro/bordas da pupila + bordas da íris)"
                           TextWrapping="Wrap" MaxWidth="250"/>
              </Button.ToolTip>
              <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource BotaoPrincipalStyle}">
                  <Setter Property="Visibility" Value="Visible"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsAlignmentActive}" Value="True">
                      <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Button.Style>
            </Button>

            <!-- 🎯 Botão Cancelar (visível durante calibração) -->
            <Button Content="❌ Cancelar"
                    Command="{Binding CancelAlignmentCommand}"
                    Height="36"
                    Margin="0,0,0,8">
              <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource BotaoSecundarioStyle}">
                  <Setter Property="Visibility" Value="Collapsed"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsAlignmentActive}" Value="True">
                      <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Button.Style>
            </Button>

            <!-- 🎯 Botão Confirmar (visível após 5 cliques) -->
            <Button Content="✅ Confirmar Calibração"
                    Command="{Binding ConfirmAlignmentCommand}"
                    Height="40"
                    Margin="0,0,0,8">
              <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource BotaoPrincipalStyle}">
                  <Setter Property="Visibility" Value="Collapsed"/>
                  <Style.Triggers>
                    <!-- Aparece quando fase é ManualAdjust ou Completed -->
                    <DataTrigger Binding="{Binding IsManualAdjustPhase}" Value="True">
                      <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Button.Style>
            </Button>

            <!-- 🎯 NOVO: Painel de Ajuste Fino (visível após 5 cliques) -->
            <Border Background="White"
                    BorderBrush="#E3E9DE"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="12"
                    Margin="0,0,0,16">
              <Border.Style>
                <Style TargetType="Border">
                  <Setter Property="Visibility" Value="Collapsed"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsManualAdjustPhase}" Value="True">
                      <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Border.Style>
              <StackPanel>
                <TextBlock Text="🎚️ Ajuste Fino Avançado"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Foreground="#3F4A3D"
                           Margin="0,0,0,12"/>

                <!-- SECÇÃO: Escalas Globais -->
                <TextBlock Text="📏 Escalas Globais" FontSize="12" FontWeight="SemiBold" Foreground="#3F4A3D" Margin="0,0,0,8"/>
                
                <TextBlock Text="Escala Horizontal" FontSize="11" Foreground="#5A6558" Margin="0,0,0,4"/>
                <Slider Minimum="0.3" Maximum="3.0" Value="{Binding OverlayService.ScaleX, Mode=TwoWay}"
                        TickFrequency="0.1" IsSnapToTickEnabled="False" Margin="0,0,0,8"/>

                <TextBlock Text="Escala Vertical" FontSize="11" Foreground="#5A6558" Margin="0,0,0,4"/>
                <Slider Minimum="0.3" Maximum="3.0" Value="{Binding OverlayService.ScaleY, Mode=TwoWay}"
                        TickFrequency="0.1" IsSnapToTickEnabled="False" Margin="0,0,0,8"/>

                <!-- SECÇÃO: Posicionamento -->
                <TextBlock Text="📍 Posicionamento" FontSize="12" FontWeight="SemiBold" Foreground="#3F4A3D" Margin="0,8,0,8"/>
                
                <TextBlock Text="Posição Horizontal" FontSize="11" Foreground="#5A6558" Margin="0,0,0,4"/>
                <Slider Minimum="-500" Maximum="500" Value="{Binding OverlayService.TranslateX, Mode=TwoWay}"
                        TickFrequency="10" IsSnapToTickEnabled="False" Margin="0,0,0,8"/>

                <TextBlock Text="Posição Vertical" FontSize="11" Foreground="#5A6558" Margin="0,0,0,4"/>
                <Slider Minimum="-500" Maximum="500" Value="{Binding OverlayService.TranslateY, Mode=TwoWay}"
                        TickFrequency="10" IsSnapToTickEnabled="False" Margin="0,0,0,8"/>

                <TextBlock Text="Rotação (graus)" FontSize="11" Foreground="#5A6558" Margin="0,0,0,4"/>
                <Slider Minimum="-180" Maximum="180" Value="{Binding OverlayService.Rotation, Mode=TwoWay}"
                        TickFrequency="5" IsSnapToTickEnabled="False" Margin="0,0,0,8"/>

                <!-- SECÇÃO: Escalas por Quadrante (Assimetrias) -->
                <TextBlock Text="🔧 Escalas por Quadrante (Assimetrias)" FontSize="12" FontWeight="SemiBold" Foreground="#3F4A3D" Margin="0,8,0,8"/>
                <TextBlock Text="Para íris cortadas ou assimétricas" FontSize="10" FontStyle="Italic" Foreground="#7A8578" Margin="0,0,0,8"/>
                
                <TextBlock Text="Quadrante 1 (0-90° - Direita Superior)" FontSize="11" Foreground="#5A6558" Margin="0,0,0,4"/>
                <Slider Minimum="0.5" Maximum="2.0" Value="{Binding OverlayService.EscalaQuadrante1, Mode=TwoWay}"
                        TickFrequency="0.05" IsSnapToTickEnabled="False" Margin="0,0,0,8"/>

                <TextBlock Text="Quadrante 2 (90-180° - Esquerda Superior)" FontSize="11" Foreground="#5A6558" Margin="0,0,0,4"/>
                <Slider Minimum="0.5" Maximum="2.0" Value="{Binding OverlayService.EscalaQuadrante2, Mode=TwoWay}"
                        TickFrequency="0.05" IsSnapToTickEnabled="False" Margin="0,0,0,8"/>

                <TextBlock Text="Quadrante 3 (180-270° - Esquerda Inferior)" FontSize="11" Foreground="#5A6558" Margin="0,0,0,4"/>
                <Slider Minimum="0.5" Maximum="2.0" Value="{Binding OverlayService.EscalaQuadrante3, Mode=TwoWay}"
                        TickFrequency="0.05" IsSnapToTickEnabled="False" Margin="0,0,0,8"/>

                <TextBlock Text="Quadrante 4 (270-360° - Direita Inferior)" FontSize="11" Foreground="#5A6558" Margin="0,0,0,4"/>
                <Slider Minimum="0.5" Maximum="2.0" Value="{Binding OverlayService.EscalaQuadrante4, Mode=TwoWay}"
                        TickFrequency="0.05" IsSnapToTickEnabled="False" Margin="0,0,0,8"/>
              </StackPanel>
            </Border>

            <!-- Instruções Contextuais -->
            <Border Background="#FFECB3"
                    BorderBrush="#D4A849"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="12"
                    Margin="0,0,0,0">
              <Border.Style>
                <Style TargetType="Border">
                  <!-- Padrão: VISÍVEL (muda para Collapsed apenas se vazio) -->
                  <Setter Property="Visibility" Value="Visible"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding AlignmentInstructionText}" Value="{x:Null}">
                      <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding AlignmentInstructionText}" Value="">
                      <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Border.Style>
              <TextBlock Text="{Binding AlignmentInstructionText}"
                         TextWrapping="Wrap"
                         FontSize="12"
                         FontWeight="Medium"
                         Foreground="#5A4A20"
                         LineHeight="18"/>
            </Border>
          </StackPanel>
        </Border>
      </Grid>
      </ScrollViewer> <!-- ✅ Fechar ScrollViewer -->
    </Border>
  </Grid>
</UserControl>


