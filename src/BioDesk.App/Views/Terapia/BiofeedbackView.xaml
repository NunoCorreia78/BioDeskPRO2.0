<UserControl
    x:Class="BioDesk.App.Views.Terapia.BiofeedbackView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:BioDesk.ViewModels.UserControls.Terapia;assembly=BioDesk.ViewModels"
    xmlns:controls="clr-namespace:BioDesk.App.Controls"
    d:DataContext="{d:DesignInstance Type=vm:BiofeedbackViewModel}"
    mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="500"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="1*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!-- ESQUERDA: Histórico de Sessões + Botão Iniciar -->
        <Border Grid.Column="0"
                Background="{DynamicResource Cartao}"
                BorderBrush="{DynamicResource Borda}"
                BorderThickness="2"
                CornerRadius="8"
                Padding="15">
            <DockPanel>
                <!-- Cabeçalho + Botão Iniciar Sessão -->
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,15">
                    <TextBlock Text="BIOFEEDBACK AUTÓNOMO"
                               FontSize="16" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextoPrincipal}"
                               Margin="0,0,0,15"/>

                    <!-- Botão Iniciar Sessão Autónoma -->
                    <Button Content="🔄 Iniciar Sessão Biofeedback"
                            Command="{Binding IniciarSessaoCommand}"
                            Height="45"
                            Margin="0,0,0,10"
                            Background="#9CAF97"
                            Foreground="White"
                            FontSize="15"
                            FontWeight="SemiBold"
                            BorderThickness="0"
                            Cursor="Hand"
                            ToolTip="Abre modal para sessão autónoma: scan ressonantes → emissão → repetir">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#879B83"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <TextBlock Text="💡 Sessão loop: scan → emissão → re-scan → repetir (até parar)"
                               FontSize="11"
                               Foreground="{DynamicResource TextoSecundario}"
                               Margin="0,0,0,15"/>

                    <TextBlock Text="HISTÓRICO DE SESSÕES"
                               FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextoPrincipal}"
                               Margin="0,0,0,8"/>
                </StackPanel>

                <!-- DataGrid Histórico -->
                <Border BorderBrush="#E3E9DE"
                        BorderThickness="1"
                        CornerRadius="4">
                    <DataGrid ItemsSource="{Binding SessionHistory}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              HeadersVisibility="Column"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              AlternatingRowBackground="#F9FAFB"
                              RowHeight="32">
                        <DataGrid.Columns>
                            <!-- Data Sessão -->
                            <DataGridTextColumn Width="140"
                                              Binding="{Binding DataSessao, StringFormat='dd/MM/yyyy HH:mm'}"
                                              Header="Data"/>

                            <!-- Modo -->
                            <DataGridTextColumn Width="110"
                                              Binding="{Binding Modo}"
                                              Header="Modo"/>

                            <!-- Duração -->
                            <DataGridTextColumn Width="100"
                                              Binding="{Binding DuracaoMinutos, StringFormat='{}{0} min'}"
                                              Header="Duração"/>

                            <!-- Ciclos -->
                            <DataGridTextColumn Width="80"
                                              Binding="{Binding Ciclos}"
                                              Header="Ciclos"/>

                            <!-- Status -->
                            <DataGridTextColumn Width="*"
                                              Binding="{Binding Status}"
                                              Header="Status"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </DockPanel>
        </Border>

        <!-- DIREITA: Controlos Unificados + Progresso -->
        <ScrollViewer Grid.Column="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- Painel de Progresso em Tempo Real -->
            <Border Background="{DynamicResource Cartao}"
                    BorderBrush="{DynamicResource Borda}"
                    BorderThickness="2"
                    CornerRadius="8"
                    Padding="15"
                    Margin="0,0,0,15"
                    Visibility="{Binding SessaoEmAndamento, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="⚡ SESSÃO BIOFEEDBACK"
                               FontSize="14" FontWeight="SemiBold"
                               Foreground="#9CAF97"
                               Margin="0,0,0,10"/>

                    <!-- Ciclo Atual -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Ciclo:"
                                   FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextoPrincipal}"
                                   Width="90"/>
                        <TextBlock Text="{Binding CicloAtual}"
                                   FontSize="12"
                                   Foreground="{DynamicResource TextoSecundario}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- Status -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Status:"
                                   FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextoPrincipal}"
                                   Width="90"/>
                        <TextBlock Text="{Binding Status}"
                                   FontSize="12"
                                   Foreground="{DynamicResource TextoSecundario}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- Tempo Restante -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Tempo restante:"
                                   FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextoPrincipal}"
                                   Width="90"/>
                        <TextBlock Text="{Binding TempoRestanteSegundos}"
                                   FontSize="12"
                                   Foreground="{DynamicResource TextoSecundario}"/>
                        <TextBlock Text="s"
                                   FontSize="12"
                                   Foreground="{DynamicResource TextoSecundario}"
                                   Margin="2,0,0,0"/>
                    </StackPanel>

                    <!-- Barra de Progresso -->
                    <ProgressBar Value="{Binding ProgressoPercentual}"
                                 Maximum="100"
                                 Height="20"
                                 Background="#E3E9DE"
                                 Foreground="#9CAF97"/>
                    <TextBlock Text="{Binding ProgressoPercentual, StringFormat='{}{0:F1}%'}"
                               FontSize="11"
                               Foreground="{DynamicResource TextoSecundario}"
                               HorizontalAlignment="Center"
                               Margin="0,3,0,0"/>
                </StackPanel>
            </Border>

            <controls:TerapiaControlosUserControl x:Name="TerapiaControlos"
                                                 VoltagemV="5.0"
                                                 DuracaoTotalMinutos="30"
                                                 TempoFrequenciaSegundos="10"
                                                 AjusteHz="0"
                                                 TextoBotao="Terapia Rápida"
                                                 IniciarClick="TerapiaControlos_IniciarClick"
                                                 PararClick="TerapiaControlos_PararClick"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
