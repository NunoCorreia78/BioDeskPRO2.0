<UserControl
    x:Class="BioDesk.App.Views.Terapia.ProgramasView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:BioDesk.ViewModels.UserControls.Terapia;assembly=BioDesk.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:ProgramasViewModel}"
    mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox
                Width="400"
                Margin="0,0,10,0"
                Text="{Binding Search, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                ToolTip="Pesquisar protocolos (filtragem automática)" />
            <TextBlock VerticalAlignment="Center" Foreground="Gray" Text="Use Ctrl+Clique ou Shift+Clique para selecionar múltiplos" />
        </StackPanel>

        <GroupBox Grid.Row="1" Grid.Column="0" Header="Programas (1.272 disponíveis)">
            <ListBox
                Margin="10"
                SelectionMode="Extended"
                ItemsSource="{Binding Programs}"
                SelectedItem="{Binding SelectedProgram, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <EventSetter Event="Selected" Handler="ListBoxItem_Selected" />
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </GroupBox>

        <GroupBox Grid.Row="1" Grid.Column="2" Header="Passos do programa">
            <DockPanel Margin="10">
                <Button
                    DockPanel.Dock="Top"
                    Margin="0,0,0,10"
                    HorizontalAlignment="Left"
                    Width="180"
                    Height="35"
                    Content="Iniciar Terapia Local"
                    Command="{Binding IniciarTerapiaLocalCommand}"
                    Background="#9CAF97"
                    Foreground="White"
                    FontWeight="SemiBold"
                    ToolTip="Abre modal para terapia local com emissão Hz direta e controlo voltagem 0-12V" />
                <DataGrid
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    HeadersVisibility="Column"
                    IsReadOnly="True"
                    ItemsSource="{Binding SelectedProgramSteps}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="50" Binding="{Binding Index}" Header="#" />
                        <DataGridTextColumn Width="120" Binding="{Binding Hz, StringFormat='{}{0:N1} Hz'}" Header="Hz" />
                        <DataGridTextColumn Width="80" Binding="{Binding Duty, StringFormat='{}{0:N0}%'}" Header="Duty" />
                        <DataGridTextColumn Width="90" Binding="{Binding Seconds}" Header="Segundos" />
                        <DataGridTextColumn Width="*" Binding="{Binding Notes}" Header="Notas" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>
    </Grid>
</UserControl>
