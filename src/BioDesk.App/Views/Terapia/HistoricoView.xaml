<UserControl
    x:Class="BioDesk.App.Views.Terapia.HistoricoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:BioDesk.ViewModels.Windows;assembly=BioDesk.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:HistoricoViewModel}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Botão para abrir Histórico Completo -->
        <Button
            Grid.Row="0"
            Width="250"
            Height="40"
            Margin="10,10,10,5"
            HorizontalAlignment="Right"
            Content="📊 Ver Histórico Completo"
            Click="OnVerHistoricoCompleto"
            Style="{StaticResource PrimaryButtonStyle}"
            FontWeight="SemiBold" />

    <GroupBox Grid.Row="1" Margin="10,5,10,10" Header="Historico de sessoes (legado)">
        <DataGrid
            Margin="10"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            HeadersVisibility="Column"
            IsReadOnly="True"
            ItemsSource="{Binding Sessions}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="160"
                    Binding="{Binding Date, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"
                    Header="Data" />
                <DataGridTextColumn Width="120" Binding="{Binding Seed}" Header="Seed" />
                <DataGridTextColumn Width="140" Binding="{Binding Rng}" Header="RNG" />
                <DataGridTextColumn Width="120" Binding="{Binding Iterations}" Header="Iteracoes" />
                <DataGridTextColumn Width="*" Binding="{Binding Summary}" Header="Resumo" />
            </DataGrid.Columns>
        </DataGrid>
    </GroupBox>
    </Grid>
</UserControl>
