<UserControl x:Class="BioDesk.App.Controls.QuickPhraseButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">

    <UserControl.Resources>
        <!-- Estilo para botões de frases rápidas -->
        <Style x:Key="QuickPhraseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F5EE"/>
            <Setter Property="BorderBrush" Value="#9CAF97"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#3F4A3D"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="true">
                            <StackPanel Orientation="Horizontal" 
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}">
                                <!-- Ícone de raio para indicar "rápido" -->
                                <TextBlock Text="⚡" 
                                           FontSize="14" 
                                           Margin="0,0,6,0"
                                           VerticalAlignment="Center"/>
                                <ContentPresenter x:Name="contentPresenter"
                                                Focusable="False"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                RecognizesAccessKey="True"
                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="#E8F0E6"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="#879B83"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="#9CAF97"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="#879B83"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="#F0F0F0"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="#D0D0D0"/>
                                <Setter Property="Foreground" Value="#A0A0A0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para separador -->
        <Style x:Key="SeparatorStyle" TargetType="Rectangle">
            <Setter Property="Fill" Value="#E3E9DE"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="0,8"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="⚡ Frases Rápidas"
                       FontSize="13" 
                       FontWeight="SemiBold" 
                       Foreground="#3F4A3D"/>
            <TextBlock Text="(um clique para preencher automaticamente)"
                       FontSize="11" 
                       Foreground="#5A6558"
                       Opacity="0.8"
                       Margin="8,1,0,0"/>
        </StackPanel>

        <!-- Separador -->
        <Rectangle Grid.Row="1" Style="{StaticResource SeparatorStyle}"/>

        <!-- Botões de frases rápidas -->
        <ScrollViewer Grid.Row="2" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      MaxHeight="150">
            <ItemsControl x:Name="ButtonsContainer" 
                          ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Phrases}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource QuickPhraseButtonStyle}"
                                Content="{Binding}"
                                Command="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=PhraseClickedCommand}"
                                CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>